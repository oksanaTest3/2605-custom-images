jobs:
  include:
    - stage: create image
      os: linux
      dist: focal
      language: generic
      vm:
        use: 2705-image6
        create: 2705-image6
      before_install:
        - sudo apt-get -y install libxml2-dev
      addons:
        apt:
          update: true

    - stage: use custom image
      vm:
        use: 2705-image6 #available
      script:
        - echo "Running job with custom image"
