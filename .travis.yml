jobs:
  include:
    - stage: "Tests"
      name: "Create custom image3"
      vm:
        use:
          name: 2605-image
      script:
        - echo "Checking if custom file exists"
        - sudo cat /my-custom-dir/ok_file.txt
    - stage: "Tests"
      name: "Use custom image"
      vm:
        use:
          name: 2605-image2
      script:
      	- echo "Checking if custom file exists"
       	- sudo cat /my-custom-dir/ok_file.txt
